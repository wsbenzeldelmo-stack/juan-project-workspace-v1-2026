const {validSession,sameOrigin,headers,csrfFor,COOKIE}=require('./_security');
module.exports=async function(req,res){headers(res);if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!sameOrigin(req)||!validSession(req))return res.status(401).json({error:'Authentication required'});const cookie=(req.headers.cookie||'').split(';').map(x=>x.trim()).find(x=>x.startsWith(COOKIE+'='))?.slice(COOKIE.length+1)||'';if(req.headers['x-csrf-token']!==csrfFor(cookie))return res.status(403).json({error:'CSRF validation failed'});const prompt=String(req.body?.prompt||'').trim();if(!prompt||prompt.length>6000)return res.status(400).json({error:'Invalid prompt'});const key=process.env.GEMINI_API_KEY;if(!key)return res.status(503).json({error:'Gemini is not configured'});const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${encodeURIComponent(key)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}]})});const data=await r.json();if(!r.ok)return res.status(r.status).json({error:'AI request failed'});return res.status(200).json({text:data?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||''});};
